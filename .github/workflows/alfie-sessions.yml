name: Alfie Sessions Validation

on:
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Validate Alfie sessions setup
        run: ./scripts/validate_sessions.sh HEAD
